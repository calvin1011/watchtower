[tool.ruff]
target-version = "py312"
line-length = 100
exclude = ["__pycache__", ".venv", "venv"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
ignore = ["E501"]  # Line too long - handled by formatter
